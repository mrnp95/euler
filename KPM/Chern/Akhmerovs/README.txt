Instructions to run the Jupyter notebooks

1. Install conda from https://conda.io/miniconda.html
2. Create the conda environment with `$ conda create -f environment.yml`
3. Link the conda environment to Jupyter with `$ python -m ipykernel install --user --name kpm_ti --display-name "kpm_ti"`.
